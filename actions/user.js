class User {
    constructor() {
        this.stor = new Loki("");
    }
    // 连接到空间
    // :Channel
    connect(name) {
        let ch = new Channel();
        
        return ch;
    }
    // 获取空间信息
    // :[]GSpaceDetail
    getGSpaceDetail(qOpt) {
    }
    // 获取物品信息
    // :[]ItemDetail
    getItemDetail(qOpt) {
    }
}
