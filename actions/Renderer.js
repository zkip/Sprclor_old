class Renderer {
    bindRenderTarget(d) {
        this.renderTarget = d;
    }
    // 绘制实现
    render() {}
    // 连接到通道
    // Channel:
    connect(ch){}
}
class ThreeRenderer extends Renderer {
    render() {
    }
}
